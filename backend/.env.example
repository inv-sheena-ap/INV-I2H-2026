# Copy to .env and adjust. Required for local run (no Docker).
# Docker Compose sets DATABASE_URL automatically.

DATABASE_URL=mysql+pymysql://app:apppass@localhost:3307/ecommerce

# Optional: JWT secret (change in production)
# SECRET_KEY=your-secret-key
